[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: testes end-to-end que precisam de Postgres + Neo4j reais (skip por default; rodar via `pytest -m e2e`)
addopts = -v --tb=short -m "not e2e"
